import { expect } from 'chai'

import SuffixTree from '../src';

describe('Suffix Tree', () => {

  it('Run without exploding', (done) => {
    const suffixTree = new SuffixTree('string')
    done()
  })

})
